<p id="notice"><%= notice %></p>
<%= link_to 'Add New Image to Database', adminform_path %>
<%= link_to 'ActiveAdmin', new_product_path %>
  <div class="card-deck-wrapper">
    <div class="card-deck">
      <div class="row">
        <% @products.each do |product| %>
          <%= render product %>
        <% end %>
      </div>
    </div>
</div>
